version: '3.8'
services:
  kuma-heartbeat:
    image: tano26/kuma-heartbeat:latest
    container_name: kuma-heartbeat
    restart: always
    environment:
      - UPTIME_KUMA_URL=
      - INTERVAL_MINUTES=5
